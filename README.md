# tencentyun-seckill

腾讯云轻量应用服务器秒杀脚本：扫码登录 → 库存检测 → 并发抢购 → 抢到弹付款。
目标商品：`featured-202607` 会场轻量 4 核 4G（`bundle_budget_mc_lg4_01`）。

```
先跑 get_cookies.py 扫码登录（含人脸确认）
再跑 snap_up_server.py 等开抢（提前 3 分钟启动）
```

## 文件说明

| 文件 | 作用 |
|---|---|
| `get_cookies.py` | Playwright 扫码登录，保存 `cookies.json`，同步活动 URL + 人脸状态到 `config.json` |
| `snap_up_server.py` | 抢购主程序：自检 → 对时等待 → 提前开打 → 并发重试 → 成功弹付款 |

运行产生的文件（**不要上传到公开库**）：`cookies.json`（登录态）、`config.json`（自动同步配置）、`logs/`（运行日志）。

## 版本差异：备份版 vs 当前版

`backup_20260909_163446/` 是初版快照，当前版是重构 + 9-10、9-11 三场实战迭代后的版本。

### get_cookies.py

| | 备份版 | 当前版 |
|---|---|---|
| 登录目标页 | 写死旧活动 URL，期号一换就崩 | 跳到任意 `act/pro/*` 即停，自动捕获 |
| 结束方式 | `page.pause()` 人工卡住 | 自动关闭浏览器 |
| 活动 URL 同步 | 无（抢购脚本里写死） | 写入 `config.json`，抢购脚本自动读取 |
| 人脸核身 | 无（开打才知道要核身，已来不及） | 登录后当场查 `ever_pass`，没过直接跳验证页等你做完 |
| 状态传递 | 无 | 人脸状态 + 时间写入 `config.json["facecheck"]` |

### snap_up_server.py

| | 备份版 | 当前版 |
|---|---|---|
| 配置 | 散落在代码各处 | 顶部 `CONFIG` 集中配置 |
| `x-csrf-token` | 手填硬编码（过期就 400） | `skey` 自动算 `g_tk`，手填仅兜底 |
| 查/买 ID | 查和买用的两期抓包拼在一起（`activity_id` 都不一致） | 已统一，注释标明每期核对 |
| 重试 | 开打只打一轮并发，打完收工 | 30 秒窗口循环：成功即停 |
| 错误码 | 不分类，失败就刷屏 | `1100202`人脸/`CSRF`掉线/`1101411`未付单→秒停；`408`→退避；`1100205`繁忙→继续 |
| 并发节奏 | 3 并发零间隔 | 首轮直打，轮间 0.35 秒 + 抖动 |
| 下单超时 | 10 秒（拥塞时 30 秒只打出一轮） | 3 秒快速失败 |
| 对时 | 每轮 poll 都 `HEAD` 服务器 | 提前 60 秒一次性校准，之后零网络等待 |
| 日志 | 只打印终端，复盘靠截图 | 落盘 `logs/snap_*.log`，一轮一行摘要 + 终场统计 |
| 查库存地域 | 写死 `[1,4,8]` | 从页面 `regionMap` 拉全量，失败回退 |
| 抢到之后 | 只打印一行 | 提取订单号/付款链接，自动弹付款页 |
| 有未付单 | 死循环报 `1101411` | 停手 + 自动弹订单页 |

### 实战沉淀的返回码（`do-goods`）

| 码 | 含义 | 脚本动作 |
|---|---|---|
| `0` | 下单成功 | 停，弹付款 |
| `1100202` | 要人脸核身 | 停（开抢前预检已提前预警） |
| `1101411` | 有未支付订单占坑 | 停，弹订单页（先付/先撤才能再抢） |
| `408` | 频率过快 | 退避 1.5 秒继续 |
| `1100205` | 系统繁忙 | 继续 |
| `1100136` | 不在抢购时间 | 继续（提前开枪的正常回音） |
| `CSRF-ERROR` / `NOT-LOGINED` | 会话失效 | 停，重新扫码 |

## 使用说明

### 环境

```bash
pip install requests playwright
playwright install chromium
```

### 步骤

1. **扫码登录**：`python get_cookies.py`，浏览器弹出来后扫码，顺手把人脸核身做了（没过会直接跳验证页），看到“浏览器已关闭”就行。
2. **核对当期参数**：打开 `snap_up_server.py` 顶部 `CONFIG`，确认 `SECKILL_TIME_STR`、`ACTIVITY_ID`、`CHECK_ACT_ID`、`BUY_ACT_ID`、`BUSINESS.id` 是当期的（活动页每天可能变，需抓包或 Playwright 重采）。
3. **提前 3 分钟运行**：`python snap_up_server.py`，看到 `✅ 人脸核身已通过` + `🕒 钟差已校准` 就可以等了，日志在 `logs/` 里。
4. **抢到付款**：脚本自动弹付款页，尽快付（超时订单会被释放）；若提示有未付单，先去订单中心处理。

## 注意事项

1. **每次开抢前重扫码**：cookie 有有效期，且每次登录都会刷新 `skey`（token 是现算的，不用手填）。
2. **人脸提前做**：开打时才报 `1100202` 神仙难救，登录流程里就会卡你一次。
3. **`408` 连片是 IP 级限流**：脚本降速救不了，先切出口 IP 或冷却；单发探测回到 `1100136` 即解封。当天高频 hammer 过的号/IP，第二天都可能还在小黑屋。
4. **`1101411` 是占坑不是没抢到**：说明已有未支付订单（超时没回包也可能建单成功），先付/先撤，否则所有新单全被挡。
5. **敏感文件勿传**：`cookies.json`、`config.json`、`logs/` 含登录态和订单号，本库只收两个 `.py`。
6. **每期必核对**：`ACTIVITY_ID`、`CHECK_ACT_ID`、`BUY_ACT_ID`、`BUSINESS.id`、开抢时间，会场一换就可能全变。
7. **超时≠失败**：网络超时时服务端可能已建单，看到超时多先去订单中心看一眼，别无脑重抢（会撞 `1101411`）。
8. 仅供个人学习研究，抢到与否看库存和手速，脚本只解决“快”和“不断线”。
